ЗАДАНИЕ №3
Этап 3. Регрессия: рекуррентные нейросети (LSTM)
Цель
Построить и обучить рекуррентную нейросеть с долгой краткосрочной памятью
(LSTM) для учёта временной структуры данных. Сравнить результаты с полносвязными
моделями из этапа 2.

1.
o

ЗАДАЧИ
Подготовка последовательностей
Загрузить обработанные данные и снова провести

стандартизацию

признаков.
o
Преобразовать данные в формат supervised learning: для каждого временного
шага создавать окно истории фиксированной длины (например, 24 предыдущих
наблюдения, что соответствует 4 часам с интервалом 10 минут).
o
Сформировать массивы X (samples, timesteps, features) и y (целевое значение
на следующий шаг).
o
Разделить на обучающую, валидационную и тестовую выборки с
сохранением хронологии.

Построение модели LSTM
o
Создать модель с одним или двумя слоями LSTM. Количество юнитов
подобрать экспериментально (например, 50–100).
o
Добавить выходной полносвязный слой с линейной активацией.
o
Компилировать с оптимизатором Adam (или RMSprop) и функцией
потерь mse.
o
Обучить модель с валидацией и EarlyStopping.
2.

3.
o
o
o

Оценка качества
Рассчитать RMSE и MAE на тестовой выборке.
Построить график предсказанных значений в сравнении с истинными.
Сравнить полученные метрики с лучшей моделью из этапа 2.

Анализ влияния длины окна
o
Провести эксперимент с разной длиной окна истории (например, 12, 24, 48
временных шагов). Сравнить качество и сделать вывод.
4.

o

Сохранение модели
Сохранить лучшую LSTM-модель (lstm_best.h5).



ВХОДНЫЕ ДАННЫЕ
Обработанные данные из этапа 1.

5.

ОБЩИЕ ТРЕБОВАНИЯ К ВЫПОЛНЕНИЮ ПРОЕКТА

Все этапы должны быть реализованы на языке Python с использованием
библиотек Keras/TensorFlow, pandas, numpy, matplotlib, scikit-learn.

Для каждого этапа рекомендуется создавать отдельную папку или notebook.

На каждом этапе необходимо сохранять промежуточные результаты (модели,
обработанные данные) для использования в следующих этапах.

Отчёты по этапам сдаются в виде Jupyter notebook с подробными
комментариями и выводами. Notebook должен быть исполняемым (можно проверить
результаты).






РЕЗУЛЬТАТ РАБОТЫ
Jupyter Notebook с кодом, описанием и результатами.
Сохранённая LSTM-модель.
Отчет с полученными результатами







КРИТЕРИИ ОЦЕНКИ
Корректное создание последовательностей для обучения.
Обоснованный выбор гиперпараметров LSTM.
Проведение сравнительного анализа с MLP/DNN.
Анализ влияния длины окна на точность прогноза.
Чёткость выводов.

